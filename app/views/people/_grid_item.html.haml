- context ||= :general
%li
  - if grid_item.photo.file?
    = link_to image_tag(grid_item.photo.url(:medium)), person_path(grid_item), :class => 'avatar'
  %h3
    =link_to grid_item.name, person_path(grid_item)
